{
  "name": "haus-card",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lovelace card for HAUS, bundled inside the integration",
  "scripts": {
    "build": "rollup -c && node scripts/copy-artifact.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test",
    "capture": "node scripts/capture-cards.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/plugin-typescript": "^12.1.2",
    "happy-dom": "^20.12.0",
    "lit": "^3.2.1",
    "rollup": "^4.34.6",
    "tslib": "^2.8.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
